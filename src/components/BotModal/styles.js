import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  maxWidth: {
    width: "100%",
    marginBottom: 10,
  },
  autoFinishWrapper: {
    display: "flex",
    height: "60px",
    gap: "30px",
  },
}));
